CREATE MATERIALIZED VIEW default.heatmaps_mv TO default.writable_heatmaps (`session_id` String, `team_id` Int64, `distinct_id` String, `timestamp` DateTime64(6, 'UTC'), `x` Int16, `y` Int16, `scale_factor` Int16, `viewport_width` Int16, `viewport_height` Int16, `pointer_target_fixed` Bool, `current_url` String, `type` LowCardinality(String), `_timestamp` Nullable(DateTime), `_offset` UInt64, `_partition` UInt64) AS SELECT session_id, team_id, distinct_id, timestamp, x, y, scale_factor, viewport_width, viewport_height, pointer_target_fixed, current_url, type, _timestamp, _offset, _partition FROM default.kafka_heatmaps