CREATE MATERIALIZED VIEW default.events_dead_letter_queue_mv TO default.events_dead_letter_queue (`id` UUID, `event_uuid` UUID, `event` String, `properties` String, `distinct_id` String, `team_id` Int64, `elements_chain` String, `created_at` DateTime64(6, 'UTC'), `ip` String, `site_url` String, `now` DateTime64(6, 'UTC'), `raw_payload` String, `error_timestamp` DateTime64(6, 'UTC'), `error_location` String, `error` String, `tags` Array(String), `_timestamp` Nullable(DateTime), `_offset` UInt64) AS SELECT id, event_uuid, event, properties, distinct_id, team_id, elements_chain, created_at, ip, site_url, now, raw_payload, error_timestamp, error_location, error, tags, _timestamp, _offset FROM default.kafka_events_dead_letter_queue