CREATE MATERIALIZED VIEW default.session_recording_events_mv TO default.writable_session_recording_events (`uuid` UUID, `timestamp` DateTime64(6, 'UTC'), `team_id` Int64, `distinct_id` String, `session_id` String, `window_id` String, `snapshot_data` String, `created_at` DateTime64(6, 'UTC'), `_timestamp` Nullable(DateTime), `_offset` UInt64) AS SELECT uuid, timestamp, team_id, distinct_id, session_id, window_id, snapshot_data, created_at, _timestamp, _offset FROM default.kafka_session_recording_events