CREATE MATERIALIZED VIEW default.ingestion_warnings_mv TO default.ingestion_warnings (`team_id` Int64, `source` LowCardinality(String), `type` String, `details` String, `timestamp` DateTime64(6, 'UTC'), `_timestamp` Nullable(DateTime), `_offset` UInt64, `_partition` UInt64) AS SELECT team_id, source, type, details, timestamp, _timestamp, _offset, _partition FROM default.kafka_ingestion_warnings