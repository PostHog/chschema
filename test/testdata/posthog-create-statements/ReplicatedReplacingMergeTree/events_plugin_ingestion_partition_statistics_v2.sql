CREATE TABLE default.events_plugin_ingestion_partition_statistics_v2 (`topic` LowCardinality(String), `partition` UInt64, `offset` UInt64, `token` String, `distinct_id` String, `ip` Tuple(v4 IPv4, v6 IPv6), `event` String, `data_length` UInt64, `timestamp` DateTime) ENGINE = ReplicatedReplacingMergeTree('/clickhouse/tables/noshard/posthog.events_plugin_ingestion_partition_statistics_v2', '{replica}-{shard}', timestamp) PARTITION BY (topic, toStartOfDay(timestamp)) ORDER BY (topic, partition, offset) TTL timestamp + toIntervalDay(30) SETTINGS index_granularity = 8192