CREATE TABLE default.events_dead_letter_queue (`id` UUID, `event_uuid` UUID, `event` String, `properties` String, `distinct_id` String, `team_id` Int64, `elements_chain` String, `created_at` DateTime64(6, 'UTC'), `ip` String, `site_url` String, `now` DateTime64(6, 'UTC'), `raw_payload` String, `error_timestamp` DateTime64(6, 'UTC'), `error_location` String, `error` String, `tags` Array(String), `_timestamp` DateTime, `_offset` UInt64, INDEX kafka_timestamp_minmax_events_dead_letter_queue _timestamp TYPE minmax GRANULARITY 3) ENGINE = ReplicatedReplacingMergeTree('/clickhouse/tables/noshard/posthog.events_dead_letter_queue', '{replica}-{shard}', _timestamp) ORDER BY (id, event_uuid, distinct_id, team_id) TTL toDate(_timestamp) + toIntervalWeek(4) SETTINGS index_granularity = 512