CREATE TABLE default.kafka_session_recording_snapshot_item_overflow_partition_statistics (`uuid` String, `distinct_id` String, `ip` String, `site_url` String, `data` String, `team_id` Int64, `now` String, `sent_at` String, `token` String) ENGINE = Kafka('kafka:9092', 'session_recording_snapshot_item_overflow', 'partition_statistics', 'JSONEachRow') SETTINGS input_format_values_interpret_expressions = 0, kafka_skip_broken_messages = 100