# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Built binaries (statically named, not caught by *.exe)
/hclexp
/chschema

# macOS
.DS_Store

# IDE settings
.idea/
/schema-dump/
/clickhouse-docs

# Local run scratch
logs/
prod/

# Root-level scratch (anchored so testdata/examples fixtures are unaffected)
/*.sql
/posthog-schema.hcl
/coverage.out

# Test-run scratch output
test/dump/dumper_test*/
